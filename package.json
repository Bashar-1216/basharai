{
  "name": "basharai-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "build": "cd src/frontend && npx prisma generate && next build",
    "dev": "cd src/frontend && next dev"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
